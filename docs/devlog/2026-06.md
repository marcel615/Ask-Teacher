## 2026-06-06

### Issue / PR

- Issue: #13 [Feature] (post): 게시글 상세 조회
- Branch: `feature/issue-13-post-detail`
- PR: #15 [Feature] 게시글 상세 조회 API 구현
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/15
- PR 상태: MERGED

### 오늘의 작업

- Issue #13 기준으로 게시글 상세 조회 요구사항을 정리했다.
- API 경로를 기존 게시글 API 컨벤션에 맞춰 `GET /api/posts/{postId}` 복수형으로 확정했다.
- 게시글 상세 조회 범위를 `docs/current-task.md`, `docs/requirements.md`, `docs/api-spec.md`에 반영했다.
- PR #15를 `docs/review-checklist.md` 기준으로 리뷰했다.
- 리뷰 결과 `docs/api-spec.md`의 게시글 상세 조회 상태를 `구현 예정`에서 `구현됨`으로 수정했다.

### 변경 내용

- 게시글 상세 조회 API 추가
  - `GET /api/posts/{postId}`
  - 응답 필드: `userName`, `categoryName`, `title`, `content`, `createdAt`
  - 게시글 없음 시 `404 Not Found`
- 게시글 상세 조회 응답 DTO 추가
  - `PostDetailResponse`
- 게시글 상세 조회 서비스 로직 추가
  - `PostService.getPost`
- 작성자와 카테고리 정보를 함께 조회하기 위한 Repository 메서드 추가
  - `PostRepository.findWithUserAndCategoryById`
- 수동 API 확인용 `.http` 파일 추가
  - `src/main/java/com/github/marcel615/askteacher/http/post/detailPost.http`

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: 통과
- 확인 내용:
  - 게시글 상세 조회 성공 테스트
  - 존재하지 않는 게시글 조회 시 `POST_NOT_FOUND` 예외 테스트

수동 확인:

- `detailPost.http`로 수동 API 확인 완료
- 확인 API:
  - `GET /api/posts/1`
  - `GET /api/posts/999999`

### 문제 / 해결

- 문제:
  - Issue 원문에는 API 경로가 `GET /api/post/{postId}` 단수형으로 적혀 있었다.
- 해결:
  - 기존 게시글 API가 `/api/posts` 복수형을 사용하므로 `GET /api/posts/{postId}`로 확정했다.

- 문제:
  - PR 리뷰 중 `docs/api-spec.md`의 게시글 상세 조회 상태가 `구현 예정`으로 남아 있었다.
- 해결:
  - `구현됨`으로 수정하고 커밋 `475b64a docs: mark post detail API implemented`를 PR 브랜치에 push했다.

### AI 활용 기록

- Architect:
  - Issue #13 분석
  - 요구사항/API/current-task 문서 초안 작성
  - API 경로 복수형 확정 반영
  - PR #15 리뷰
  - 리뷰 결과를 반드시 수정 / 선택 개선 / 통과 항목으로 분류
  - API 명세 상태값 수정 커밋 및 push
  - devlog 기록 초안 작성
- Builder:
  - `GET /api/posts/{postId}` 구현
  - `PostDetailResponse` 추가
  - `PostService.getPost` 구현
  - `PostRepository` 상세 조회 메서드 추가
  - 게시글 상세 조회 테스트 추가
  - 수동 확인용 `.http` 파일 추가

### 다음 작업 후보

- 게시글 삭제 API
- 댓글 CRUD
- 게시글 상세 조회 Controller/WebMvc 테스트 추가 여부 검토

---

## 2026-06-06 - Issue #14 게시글 삭제 API

### Issue / PR

- Issue: #14 [Feature] (post): 게시글 삭제
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/14
- PR: #19 feat(post): add soft delete API
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/19
- PR 상태: OPEN

### 오늘의 작업

- Issue #14 기준으로 게시글 삭제 요구사항을 정리했다.
- 삭제 방식은 물리 삭제가 아닌 `Post.deleted = true` soft delete로 확정했다.
- 작성자 검증과 로그인 토큰 기반 인증/인가 구조 변경은 이번 Issue 범위에서 제외했다.
- 삭제 성공 응답은 기존 응답 구조와 맞춰 `200 OK + status/message`로 확정했다.
- `docs/current-task.md`, `docs/requirements.md`, `docs/api-spec.md`에 삭제 API 작업 범위를 반영했다.
- PR #19를 `docs/review-checklist.md` 기준으로 리뷰했다.
- 리뷰 코멘트를 PR에 남기고, 인코딩 깨짐 문제를 수정했다.
- `docs/api-spec.md`의 게시글 삭제 상태를 `구현 예정`에서 `구현됨`으로 수정했다.

### 변경 내용

- 게시글 삭제 API 추가
  - `DELETE /api/posts/{postId}`
  - 성공 시 `200 OK`
  - 응답 body는 `status/message`만 포함하고 `data`는 제외
- 게시글 삭제 서비스 로직 추가
  - 삭제되지 않은 게시글만 조회
  - 존재하지 않거나 이미 삭제된 게시글은 `POST_NOT_FOUND` 처리
  - 삭제 시 `deleted = true`, `updatedAt` 갱신
- `ApiResponse`에 data 없는 성공 응답 생성 메서드 추가
- 게시글 삭제 서비스 테스트 추가
- 수동 API 확인용 `.http` 파일 추가
  - `src/main/java/com/github/marcel615/askteacher/http/post/deletePost.http`
- API 명세 상태 수정
  - 게시글 삭제: `구현 예정` -> `구현됨`

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: PR 본문 기준 통과
- 확인 내용:
  - 게시글 삭제 성공 테스트
  - 존재하지 않는 게시글 삭제 시 `POST_NOT_FOUND` 예외 테스트
  - 이미 삭제된 게시글 삭제 시 `POST_NOT_FOUND` 예외 테스트

수동 확인:

- `deletePost.http`로 수동 API 확인 기록됨
- 확인 API:
  - `DELETE /api/posts/1`
  - `DELETE /api/posts/999999`

### 문제 / 해결

- 문제:
  - Issue 원문에는 “자신이 작성한 질문 게시글 삭제”가 포함되어 있었지만, 현재 인증/인가 구조가 아직 확정되지 않았다.
- 해결:
  - 이번 Issue에서는 작성자 검증을 제외하고, 추후 인증/인가 리팩토링 때 보완하기로 범위를 명확히 했다.

- 문제:
  - 삭제 성공 응답을 `204 No Content`로 할지, 기존 공통 응답 구조에 맞출지 결정이 필요했다.
- 해결:
  - 프로젝트 응답 구조 통일성을 우선해 `200 OK + status/message`로 확정했다.

- 문제:
  - PR 리뷰 중 `docs/api-spec.md`의 게시글 삭제 상태가 `구현 예정`으로 남아 있었다.
- 해결:
  - `구현됨`으로 수정하고 커밋 `2ef450f docs: mark post delete api implemented`를 PR 브랜치에 push했다.

- 문제:
  - PR 리뷰 코멘트가 PowerShell 인코딩 문제로 GitHub에서 깨져 보였다.
- 해결:
  - GitHub API로 기존 코멘트를 UTF-8 본문으로 다시 수정했다.

### AI 활용 기록

- Architect:
  - Issue #14 분석
  - 요구사항/API/current-task 문서 초안 작성
  - 삭제 응답 형식과 작성자 검증 제외 범위 정리
  - PR #19 리뷰
  - 리뷰 결과를 반드시 수정 / 선택 개선 / 통과 항목으로 분류
  - PR 리뷰 코멘트 작성 및 인코딩 문제 수정
  - API 명세 상태값 수정 커밋 및 push
  - devlog 기록 초안 작성
- Builder:
  - `DELETE /api/posts/{postId}` 구현
  - soft delete 로직 구현
  - `ApiResponse<Void>` 응답 처리 추가
  - 게시글 삭제 테스트 추가
  - 수동 확인용 `.http` 파일 추가

### 다음 작업 후보

- PR 리뷰의 반드시 수정 항목 반영
  - 삭제 성공 메시지 명세/구현 일치
  - Issue #14 범위 밖 `docs/prompts/quick.md` 변경 정리
- 삭제 API Controller/WebMvc 테스트 추가 여부 검토
- 인증/인가 학습 후 작성자 검증 리팩토링
- 댓글 CRUD

---

## 2026-06-07 - Issue #17 게시글 목록 조회 내림차순 개선

### Issue / PR

- Issue: #17 refactor: 게시글 목록 조회 내림차순 개선
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/17
- PR: #20 refactor: 게시글 목록 조회 createdAt 내림차순 정렬 적용
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/20
- PR 상태: OPEN

### 오늘의 작업

- Issue #17 기준으로 게시글 목록 조회 정렬 요구사항을 정리했다.
- 게시글 목록은 `createdAt DESC` 기준으로 최근 생성 게시글부터 반환하도록 범위를 확정했다.
- 검색, 페이징, 정렬 파라미터 추가는 이번 Issue 범위에서 제외했다.
- `docs/current-task.md`, `docs/requirements.md`, `docs/api-spec.md`에 게시글 목록 정렬 작업 범위를 반영했다.
- PR #20에서 게시글 목록 조회 정렬 구현과 테스트 결과를 확인했다.

### 변경 내용

- 게시글 목록 조회 정렬 적용
  - `GET /api/posts`
  - `createdAt` 기준 내림차순 정렬
  - 삭제되지 않은 게시글만 조회하는 기존 조건 유지
  - 기존 응답 필드 유지
- Repository 메서드명 기반 정렬 적용
  - `findByDeletedFalseOrderByCreatedAtDesc`
- 게시글 목록 정렬 검증 테스트 추가
- 수동 API 확인용 `.http` 파일 보강
  - `src/main/java/com/github/marcel615/askteacher/http/post/listPosts.http`

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: PR 본문 기준 통과
- 확인 내용:
  - 게시글 목록 조회 시 `createdAt DESC` 정렬 검증
  - 삭제 게시글 제외 조건 유지
  - 기존 응답 구조 유지

수동 확인:

- PR 본문 기준 직접 실행 확인 완료
- `listPosts.http`에 게시글 목록 정렬 확인 예시 보강

### 문제 / 해결

- 문제:
  - 기존 게시글 목록 조회 요구사항에는 반환 순서가 명시되어 있지 않아 조회 순서가 보장되지 않을 수 있었다.
- 해결:
  - `createdAt DESC` 기준으로 최근 생성 게시글부터 반환하도록 요구사항/API/current-task에 명시하고 구현 범위를 확정했다.

- 문제:
  - 정렬 요구사항을 추가하면서 검색, 페이징, 정렬 파라미터까지 확장될 가능성이 있었다.
- 해결:
  - 이번 Issue 범위는 생성일 내림차순 고정 정렬만 포함하고, 검색/페이징/정렬 파라미터는 제외 범위로 명시했다.

### AI 활용 기록

- Architect:
  - Issue #17 분석
  - 요구사항/API/ERD 변경 필요 여부 판단
  - ERD 변경 없음 사유 정리
  - `docs/current-task.md` 초안 작성
  - `docs/requirements.md`, `docs/api-spec.md` 문서 반영
  - devlog 기록 초안 작성
- Builder:
  - `GET /api/posts` 목록 조회 `createdAt DESC` 정렬 적용
  - 삭제 게시글 제외 조건 유지
  - 게시글 목록 정렬 테스트 추가
  - 수동 확인용 `listPosts.http` 보강

### 다음 작업 후보

- PR #20 리뷰 진행
- 게시글 목록 조회 Controller/WebMvc 테스트 추가 여부 검토
- 검색 / 페이징 기능 설계
- 댓글 CRUD

---

## 2026-06-09 - Issue #TBD JWT 기반 로그인 인증 개선

### Issue / PR

- Issue: #TBD JWT 기반 로그인 인증 개선
- Issue URL: TBD
- PR: #TBD JWT 기반 로그인 인증 개선
- PR URL: TBD
- PR 상태: 작성 전

### 오늘의 작업

- 기존 로그인 성공 응답을 JWT Access Token 발급 방식으로 변경했다.
- JWT 발급/검증을 담당하는 `JwtTokenProvider`를 추가했다.
- 요청의 `Authorization: Bearer {accessToken}` 헤더를 검증하는 JWT 인증 필터를 추가했다.
- Spring Security 설정에 JWT 인증 필터를 등록했다.
- 세션을 사용하지 않도록 `SessionCreationPolicy.STATELESS` 설정을 추가했다.
- JWT 설정을 `application.properties`에서 `application.yaml`로 전환했다.
- JWT 인증 개선 내용에 맞춰 요구사항/API 문서를 일부 수정했다.

### 변경 내용

- Gradle JWT 의존성 추가
  - `io.jsonwebtoken:jjwt-api`
  - `io.jsonwebtoken:jjwt-impl`
  - `io.jsonwebtoken:jjwt-jackson`
- 로그인 응답 변경
  - 기존 사용자 정보 응답에서 `accessToken`, `tokenType` 응답으로 변경
- JWT 인증 구성 추가
  - `JwtTokenProvider`
  - `JwtAuthenticationFilter`
- Security 설정 변경
  - `/api/auth/login`, `/api/auth/signup`은 인증 없이 접근 허용
  - 그 외 요청은 인증 필요
  - JWT 필터를 `UsernamePasswordAuthenticationFilter` 앞에 등록
  - 세션 정책을 stateless로 설정
- 설정 파일 변경
  - `application.properties` 제거
  - `application.yaml` 추가
  - `jwt.secret`, `jwt.expiration` 설정 추가
- 문서 반영
  - `docs/requirements.md`
  - `docs/api-spec.md`
- ERD 변경 없음
  - Access Token은 서버 DB에 저장하지 않으므로 테이블 변경 없음
  - Refresh Token은 이번 범위에서 제외

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: 통과
- 확인 내용:
  - Spring Application Context 로딩 성공
  - JWT 설정값 placeholder 정상 로딩
  - 기존 테스트 전체 통과

수동 확인:

- 아직 별도 API 수동 확인은 수행하지 않음
- 추후 로그인 API에서 Access Token 발급 확인 필요
- 추후 인증 필요 API에 `Authorization: Bearer {accessToken}` 헤더를 포함한 요청 확인 필요

### 문제 / 해결

- 문제:
  - `application.properties`에 YAML 문법으로 JWT 설정을 작성해 `jwt.secret` placeholder를 찾지 못했다.
- 해결:
  - 설정 파일을 `application.yaml`로 변경하고 전체 설정을 YAML 문법에 맞게 변환했다.

- 문제:
  - JWT 인증을 적용하면서 로그인 응답 스펙과 기존 문서 내용이 맞지 않게 되었다.
- 해결:
  - 로그인 응답을 Access Token 기준으로 문서화하고, 인증 헤더 사용 방식을 `docs/api-spec.md`에 추가했다.

- 문제:
  - Access Token 저장 위치에 대한 설계가 필요했다.
- 해결:
  - 현재 범위에서는 백엔드에 Access Token을 저장하지 않고, 클라이언트가 보관한 토큰을 요청마다 전송하며 서버는 서명/만료 검증만 수행하는 방식으로 정리했다.

### AI 활용 기록

- Architect:
  - JWT 인증 적용 순서 설명
  - `AuthController`, `AuthService`, `JwtTokenProvider`, `JwtAuthenticationFilter`, `SecurityConfig` 역할 분리 설명
  - Access Token 저장 방식과 Refresh Token 제외 범위 정리
  - 문서 변경 필요 범위 판단
  - devlog 기록 초안 작성
- Builder:
  - JWT 의존성 추가 방향 안내
  - `JwtTokenProvider` 구현 방향 안내
  - `JwtAuthenticationFilter` 구현 방향 안내
  - Security filter chain 등록 방식 안내
  - `application.yaml` 전환 및 테스트 검증 지원
  - `docs/requirements.md`, `docs/api-spec.md` 문서 반영 지원

### 다음 작업 후보

- 로그인 API 수동 확인
- 발급받은 Access Token으로 인증 필요 API 요청 확인
- 게시글 작성 API에서 request body의 `userId` 대신 JWT 인증 사용자 ID를 사용하도록 개선
- 게시글 수정/삭제 API의 작성자 검증을 JWT 인증 사용자 기준으로 개선
- 공개 API와 인증 필요 API의 Security 정책 정리
- Refresh Token 도입 여부 별도 Issue로 검토

---

## 2026-06-09 - Issue #TBD DTO record 리팩토링 및 게시글 인증 적용

### Issue / PR

- Issue: #TBD DTO record 리팩토링 및 게시글 인증 적용
- Issue URL: TBD
- PR: #TBD DTO record 리팩토링 및 게시글 인증 적용
- PR URL: TBD
- PR 상태: 작성 전

### 오늘의 작업

- domain DTO 클래스를 Java record로 전환했다.
- request DTO의 Bean Validation 어노테이션을 record component에 유지했다.
- 게시글 목록 응답에 `categoryName`을 추가했다.
- 게시글 상세 응답에 `postId`가 포함된 상태를 기준으로 테스트와 문서를 맞췄다.
- 공개 조회 API와 인증 필요 API의 Security 정책을 정리했다.
- 게시글 작성/수정 request body에서 `userId`를 제거했다.
- 게시글 작성/수정/삭제에서 JWT 인증 사용자 ID를 사용하도록 controller/service를 수정했다.
- JWT 인증 principal 타입을 `String`에서 `Long`으로 변경했다.
- `docs/api-spec.md`, `docs/requirements.md`의 깨진 한글을 정상 한국어 문서로 정리했다.

### 변경 내용

- DTO record 전환
  - `LoginRequest`, `LoginResponse`
  - `SignupRequest`, `SignupResponse`
  - `CategoryResponse`
  - `PostCreateRequest`, `PostCreateResponse`
  - `PostDetailResponse`, `PostListResponse`
  - `PostUpdateRequest`, `PostUpdateResponse`
- 게시글 인증 정책 변경
  - `POST /api/posts`: 인증 필요
  - `PATCH /api/posts/{postId}`: 인증 필요
  - `DELETE /api/posts/{postId}`: 인증 필요
  - `GET /api/categories`: 인증 불필요
  - `GET /api/posts`: 인증 불필요
  - `GET /api/posts/{postId}`: 인증 불필요
- 게시글 작성/수정 DTO 변경
  - request body에서 `userId` 제거
  - 작성자 검증은 Access Token의 사용자 ID 기준으로 처리
- JWT 변경
  - `JwtTokenProvider#getAuthentication()` principal을 `Long`으로 설정
  - controller에서 `@AuthenticationPrincipal Long userId` 사용
- 문서 변경
  - API 명세에 인증 필요 여부, request/response 필드 변경 반영
  - 요구사항 문서를 현재 구현 기준의 정상 한국어로 정리

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: 통과
- 확인 내용:
  - 전체 테스트 통과
  - DTO record 전환 후 컴파일 성공
  - 게시글 서비스 테스트 통과
  - SecurityConfig 테스트 통과
  - JWT principal 타입 테스트 통과
  - `git diff --check` 통과
  - 문서/게시글 DTO/controller의 깨진 문자 패턴 검색 이상 없음

수동 API 확인:

- 별도 `.http` 수동 실행은 아직 수행하지 않음
- 추후 로그인으로 Access Token 발급 후 게시글 작성/수정/삭제 API 수동 확인 필요

### 문제 / 해결

- 문제:
  - record 전환 후 기존 getter 호출부가 컴파일되지 않는 문제가 있었다.
- 해결:
  - DTO 접근을 `getXxx()`에서 record accessor인 `xxx()`로 변경했다.

- 문제:
  - 게시글 작성/수정 request body의 `userId`는 클라이언트가 조작할 수 있어 인증 기반 처리와 맞지 않았다.
- 해결:
  - request DTO에서 `userId`를 제거하고, JWT 인증 principal의 사용자 ID를 service로 전달하도록 변경했다.

- 문제:
  - `@AuthenticationPrincipal String userId`를 사용할 경우 controller마다 `Long` 변환이 필요했다.
- 해결:
  - `JwtTokenProvider`에서 principal을 `Long`으로 생성하도록 변경하고 controller에서 `@AuthenticationPrincipal Long userId`를 사용했다.

- 문제:
  - 일부 문서와 메시지에 깨진 한글이 남아 있었다.
- 해결:
  - `docs/api-spec.md`, `docs/requirements.md`를 정상 한국어 UTF-8 내용으로 정리했다.

### AI 활용 기록

- Architect:
  - DTO record 전환 범위 정리
  - 공개 API와 인증 필요 API 경계 정리
  - 게시글 작성/수정/삭제의 인증 사용자 ID 사용 방식 검토
  - `String` principal과 `Long` principal의 장단점 설명
  - API 명세와 요구사항 문서 반영 범위 정리
- Builder:
  - DTO record 전환 구현 보조
  - service/controller accessor 및 시그니처 변경 보조
  - SecurityConfig 공개/인증 정책 변경 보조
  - JWT principal `Long` 변경 보조
  - 관련 테스트 추가 및 전체 테스트 검증 보조
  - 깨진 한글 문서 정리 보조

### 다음 작업 후보

- 로그인 후 Access Token 발급 수동 확인
- Access Token으로 게시글 작성/수정/삭제 API 수동 확인
- `.http` 파일에 인증 헤더 기반 게시글 작성/수정/삭제 요청 예시 보강
- 게시글 상세 조회 응답에 `mine` 또는 `editable` 필드 추가 여부 검토
- 수정/삭제 API의 controller 통합 테스트 추가
- Refresh Token 도입 여부 별도 Issue로 검토

---

## 2026-06-27 - Issue #24 게시글 좋아요 기능

### Issue / PR

- Issue: #24 [Feature] post: 게시글 좋아요 기능
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/24
- PR: #28 [Feature] 게시글 좋아요 기능 구현
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/28
- PR 상태: OPEN
- Branch: `feature/issue-24-post-like` -> `develop`
- Commit:
  - `50d4eea` feat: add post like API
  - `fee3fba` fix: address post like review feedback

### 오늘의 작업

- 게시글 좋아요 등록/취소 API를 추가했다.
- `post_likes` Entity, Repository, Service, Controller를 추가했다.
- 게시글 목록 응답에 `likeCount`를 추가했다.
- 게시글 상세 응답에 `likeCount`, `likedByMe`를 추가했다.
- Issue #24 기준으로 `docs/current-task.md`, `docs/requirements.md`, `docs/api-spec.md`, `docs/erd.md`를 갱신했다.
- 리뷰 피드백을 PR 코멘트로 남기고, 후속 커밋에서 주요 피드백을 반영했다.

### 변경 내용

- 좋아요 API
  - `POST /api/posts/{postId}/likes`
  - `DELETE /api/posts/{postId}/likes`
- DB / Entity
  - `post_likes` 테이블 매핑 추가
  - `(post_id, user_id)` 유니크 제약 추가
  - `posts.likeCount` 필드 추가
  - `post_likes.createdAt` 추가
- 게시글 조회 응답
  - 목록 응답에 `likeCount` 추가
  - 상세 응답에 `likeCount`, `likedByMe` 추가
- 동시성 보완
  - 좋아요 수 갱신을 엔티티 read-modify-write 방식에서 DB update query 방식으로 보완
  - 같은 사용자 중복 좋아요의 unique 제약 위반을 400 응답으로 매핑
- 수동 확인
  - 좋아요 등록/취소 확인용 `.http` 파일 추가
  - 게시글 목록/상세 `.http` 인증 흐름과 응답 예시 보강

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: 통과
- 확인 내용:
  - 컴파일 성공
  - 전체 테스트 통과
  - PR 기준 직접 실행 확인 체크됨
  - 좋아요 등록/취소 `.http` 요청 흐름 추가 확인
  - 게시글 목록/상세 응답 예시에 좋아요 필드 반영 확인

수동 API 확인:

- `likePost.http`에 회원가입, 로그인, 게시글 작성, 좋아요 등록, 목록/상세 조회, 좋아요 취소, 취소 후 상세 조회 흐름을 추가했다.
- `detailPost.http`, `listPosts.http`의 인증 기반 게시글 작성 흐름과 응답 예시를 보강했다.

### 문제 / 해결

- 문제:
  - 삭제된 게시글 상세 조회가 `deleted = false` 조건 없이 조회될 수 있었다.
- 해결:
  - 삭제되지 않은 게시글만 상세 조회되도록 Repository 조회 조건을 보완했다.

- 문제:
  - 좋아요 수를 엔티티 필드에서 직접 증가/감소하면 동시 요청에서 lost update 가능성이 있었다.
- 해결:
  - `posts.likeCount`를 DB update query로 갱신하도록 수정했다.

- 문제:
  - `post_likes.createdAt`이 ERD에는 있었지만 Entity에는 없었다.
- 해결:
  - `PostLike` Entity에 `createdAt`을 추가해 ERD와 구현을 맞췄다.

- 문제:
  - 좋아요 API 성공 메시지가 API 명세와 달랐다.
- 해결:
  - Controller 응답 메시지를 `docs/api-spec.md`의 한국어 메시지와 맞췄다.

- 문제:
  - 기존 `.http` 일부 요청이 인증 방식 변경 이후의 API와 맞지 않았다.
- 해결:
  - Access Token 발급 후 `Authorization` 헤더를 사용하는 흐름으로 보강했다.

### AI 활용 기록

- Architect:
  - Issue #24 요구사항을 기준으로 requirements/API/ERD 변경 필요 여부를 판단했다.
  - `docs/current-task.md` 초안을 작성하고 승인 후 문서에 반영했다.
  - `docs/review-checklist.md` 기준으로 PR #28을 리뷰했다.
  - 반드시 수정, 선택 개선, 통과 항목으로 리뷰 결과를 분류했다.
  - 리뷰 결과를 PR 코멘트로 게시했다.
- Builder:
  - 게시글 좋아요 등록/취소 구현 보조
  - `post_likes` 도메인 구성 보조
  - 조회 DTO 필드 추가 보조
  - 좋아요 수 동시성 보완 및 리뷰 피드백 반영 보조
  - `.http` 수동 확인 파일 보강 보조

### 다음 작업 후보

- 좋아요 등록/취소 API 통합 테스트 추가
- 좋아요 수 동시성 시나리오 테스트 추가
- `likedByMe` 인증/비인증 상세 조회 케이스 테스트 추가
- 좋아요 사용자 목록 조회 필요 여부 검토
- 좋아요 기반 정렬/검색 요구사항 별도 Issue 검토

---

## 2026-06-27 - Issue #25 게시글 파일 업로드 기능

### Issue / PR

- Issue: #25 [Feature] post: 게시글 파일 업로드 기능
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/25
- PR: #29 [Feature] post: 게시글 파일 업로드 기능
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/29
- PR 상태: OPEN
- Branch: `feature/issue-25-post-file-upload` -> `develop`
- Commit:
  - `428d0c6` feat(post): support multipart file attachments
  - `a4b529e` fix(post): address file upload review comments

### 오늘의 작업

- 게시글 작성/수정 API에서 `multipart/form-data` 요청을 받을 수 있도록 변경했다.
- 게시글 첨부파일 저장을 위한 `PostFile` Entity와 Repository를 추가했다.
- 파일 저장 책임을 `PostFileStorage` 컴포넌트로 분리했다.
- 게시글 상세 조회 응답에 첨부파일 목록을 추가했다.
- Issue #25 기준으로 `docs/current-task.md`, `docs/requirements.md`, `docs/api-spec.md`, `docs/erd.md`를 갱신했다.
- PR 리뷰를 진행하고, 리뷰 코멘트에서 지적한 파일 크기 초과 응답, `fileUrl`, `.http` 확인 누락을 후속 커밋에서 반영했다.

### 변경 내용

- 게시글 API
  - `POST /api/posts` 요청을 `multipart/form-data` 기반으로 변경
  - `PATCH /api/posts/{postId}` 요청을 `multipart/form-data` 기반으로 변경
  - 요청 파트로 `categoryId`, `title`, `content`, `files`를 받도록 구성
- 첨부파일 저장
  - `post_files` 테이블 매핑용 `PostFile` Entity 추가
  - `PostFileRepository` 추가
  - `PostFileStorage`에서 파일명 생성, 저장 경로 생성, 파일 검증, 실제 저장 처리
- 첨부파일 응답
  - `PostFileResponse` 추가
  - 게시글 상세 조회 응답에 `files` 목록 추가
  - `fileUrl`을 서버 내부 절대 경로가 아니라 `/files/{storedFileName}` 형태로 반환
- 파일 접근 설정
  - `/files/**` 정적 리소스 핸들러 추가
  - `/files/**` GET 요청을 인증 없이 접근 가능하도록 Security 설정 보강
- 예외 처리
  - 허용 파일 타입 제한: `image/jpeg`, `image/png`, `image/webp`, `application/pdf`
  - 빈 파일 업로드 거부
  - 파일 크기 초과 시 400 응답 처리
  - 파일 저장 실패 시 500 응답 처리
  - `MaxUploadSizeExceededException` 핸들러 추가
- 설정 / 수동 확인
  - multipart 업로드 제한 설정 추가
  - 업로드 저장 경로와 최대 파일 크기 설정 추가
  - 업로드 산출물이 커밋되지 않도록 `uploads/`를 `.gitignore`에 추가
  - 게시글 작성/수정/상세 조회 `.http` 파일을 multipart 확인용으로 보강
  - 파일 크기 초과 확인용 `.http` 케이스 추가

### 테스트 결과 / 수동 확인 결과

```bash
./gradlew test
```

- 결과: PR 본문 기준 통과
- 원격 PR 체크: 보고된 체크 없음
- 확인 내용:
  - PR 본문에 `./gradlew test` 통과 기록
  - PR 본문에 직접 실행 확인 기록
  - 게시글 작성 multipart 요청 `.http` 추가 확인
  - 게시글 수정 multipart 요청 `.http` 추가 확인
  - 게시글 상세 조회 첨부파일 응답 예시 추가 확인
  - 허용되지 않은 파일 형식 확인용 `.http` 케이스 추가 확인
  - 빈 파일 확인용 `.http` 케이스 추가 확인
  - 파일 크기 초과 확인용 `.http` 케이스 추가 확인

수동 API 확인:

- `createPost.http`에 multipart 게시글 작성, 허용되지 않은 파일 형식, 빈 파일, 파일 크기 초과, 제목 누락 케이스를 추가했다.
- `updatePost.http`에 multipart 게시글 수정, PDF 첨부, 허용되지 않은 파일 형식, 제목 누락 케이스를 추가했다.
- `detailPost.http`에 첨부파일이 포함된 게시글 상세 조회 흐름과 응답 예시를 추가했다.

### 문제 / 해결

- 문제:
  - 파일 크기 초과 요청이 Spring multipart 제한에 먼저 걸리면 명세와 달리 500 응답으로 처리될 수 있었다.
- 해결:
  - `spring.servlet.multipart.max-file-size`, `spring.servlet.multipart.max-request-size`를 설정하고 `MaxUploadSizeExceededException`을 400 응답으로 매핑했다.

- 문제:
  - 게시글 상세 조회 응답의 `fileUrl`이 서버 내부 절대 경로를 그대로 노출할 수 있었다.
- 해결:
  - `fileUrl`을 `/files/{storedFileName}` 형태로 반환하도록 변경하고, `/files/**` 정적 리소스 제공 설정을 추가했다.

- 문제:
  - 파일 크기 초과 수동 확인 항목이 `current-task.md`에는 있었지만 `.http`에는 누락되어 있었다.
- 해결:
  - `createPost.http`에 10MB 초과 파일 업로드 확인 케이스를 추가했다.

- 문제:
  - 업로드 테스트 산출물인 `uploads/` 디렉터리가 작업 디렉터리에 남을 수 있었다.
- 해결:
  - `.gitignore`에 `uploads/`를 추가해 업로드 산출물이 커밋되지 않도록 했다.

### AI 활용 기록

- Architect:
  - Issue #25 요구사항을 기준으로 requirements/API/ERD 변경 필요 여부를 판단했다.
  - `docs/current-task.md` 초안을 작성하고 승인 후 문서에 반영했다.
  - `docs/review-checklist.md` 기준으로 PR #29를 리뷰했다.
  - 반드시 수정, 선택 개선, 통과 항목으로 리뷰 결과를 분류했다.
  - 리뷰 결과를 PR 코멘트로 게시했다.
  - 후속 커밋 반영 내용을 다시 확인하고 devlog 추가 기록 초안을 작성했다.
- Builder:
  - 게시글 파일 업로드 구현 보조
  - `PostFile` 도메인 구성 보조
  - multipart 요청 처리 보조
  - 파일 저장 컴포넌트 구성 보조
  - 파일 검증/예외 처리 보조
  - 정적 파일 접근 설정과 리뷰 피드백 반영 보조
  - `.http` 수동 확인 파일 보강 보조

### 다음 작업 후보

- 게시글 파일 업로드 Controller/WebMvc 테스트 추가
- `PostFileStorage` 단위 테스트 추가
- 파일 저장 실패 시 디스크 파일과 DB 정합성 보완
- 첨부파일 삭제 API 필요 여부 검토
- 게시글 수정 시 기존 첨부파일 유지/교체/삭제 정책 별도 Issue로 정리
- 실제 파일 내용 기반 MIME 검증 또는 확장자 검증 강화 검토
