# Devlog — 2026-09

---

## 2026-09-15 - Issue #32 API 공통 응답 타입 개선

### Issue / PR

- Issue: #32 refactor: API 공통 응답 타입 개선
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/32
- PR: #35 refactor: 성공 API 응답에서 공통 래퍼 제거 (#32)
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/35
- PR 상태: OPEN
- Branch: `feature/issue-32-api-response-refactor` → `develop`

### 오늘의 작업

- 인증·게시글·카테고리·게시글 좋아요 API의 성공 응답에서 `ApiResponse<T>` 래퍼를 제거했다.
- 후속 승인에 따라 자동 테스트를 실행하고, 변경 API의 `.http` 예시를 갱신한 뒤 수동 API를 확인했다.
- 변경된 성공 응답 형식을 `docs/api-spec.md`에 반영했다.

### 변경 내용

- 데이터가 있는 성공 응답은 기존 DTO 또는 목록을 `ResponseEntity` 본문에 직접 반환한다.
- 회원가입·게시글 작성은 201, 로그인·카테고리 조회·게시글 조회 및 수정은 200을 유지한다.
- 좋아요 등록은 200, 좋아요 취소·게시글 삭제는 204를 반환하며 세 응답 모두 본문이 없다.
- 미사용 `ApiResponse.java`를 별도 승인 후 삭제했다.
- 기존 `.http` 파일의 `response.body.data` 참조와 오래된 게시글 작성 요청 형식을 변경된 API에 맞게 갱신했다.

### 테스트 결과 / 수동 확인 결과

- `./gradlew test`: `BUILD SUCCESSFUL`. `ApiResponse.java` 삭제 후에도 재실행해 통과했다.
- 로컬 18080 포트에서 PowerShell·curl로 수동 확인했다. 회원가입·게시글 작성은 201, 로그인·카테고리·게시글 목록/상세/수정은 200이며 DTO 또는 목록이 본문에 직접 반환됐다.
- 좋아요 등록은 200, 좋아요 취소·게시글 삭제는 204였고 세 응답의 본문은 비어 있었다.
- 컨트롤러와 `.http` 파일에 `ApiResponse` 및 `response.body.data` 사용이 남아 있지 않은지 `rg`로 확인했다.
- `.http` 파일은 갱신했으며, 기록된 수동 요청 실행 방법은 PowerShell·curl이다.

### 문제 / 해결

- 첫 테스트 실행에서 sandbox 네트워크 제한으로 Gradle 배포 파일을 받지 못했다. 허용된 환경에서 다시 실행해 통과를 확인했다.
- 수동 확인용 `bootRun`은 기본 포트 8080이 사용 중이라 시작하지 못했다. 로컬 18080 포트로 실행했다.
- PowerShell 5의 `Invoke-WebRequest`가 기본 IE 파서 문제로 응답을 읽지 못했다. `-UseBasicParsing` 옵션으로 재시도했다.
- 응답 상태 코드는 `ResponseEntity`에서 직접 지정하고 `@ResponseStatus`를 제거하기로 했다.
- 처음에는 미사용 `ApiResponse.java`가 남아 있었다. 삭제 여부를 별도로 승인받은 뒤 삭제하고 테스트를 재실행했다.

### AI 활용 기록

- Architect: Issue #32의 구현 범위를 `docs/current-task.md`와 API 명세에 정리했다.
- Builder: 성공 응답 변경, `.http` 예시 갱신, 테스트·수동 확인 과정과 주요 문제를 `docs/current-work-log.md`에 기록했다.
- Reporter: Issue·PR·작업 로그를 대조해 이 devlog 추가 기록 초안을 작성했다.

### 다음 작업 후보

- PR #35 리뷰 결과 확인 및 필요한 후속 작업 정리
- 성공 상태 코드와 응답 본문 구조를 검증하는 자동 테스트를 별도 작업으로 검토
- 변경된 `.http` 요청별 실제 응답 결과를 더 구체적으로 기록하는 방식 검토

## 2026-09-16 - Issue #33 전체 API 자동 테스트 구축 및 수동 테스트 지침 전환

### Issue / PR

- Issue: #33 test: 전체 API 자동 테스트 구축 및 수동 테스트 지침 전환
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/33
- PR: #36 test: 전체 API 자동 테스트 구축 및 인증 응답 검증
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/36
- PR 상태: OPEN
- Branch: `feature/issue-33-api-automated-tests` → `develop`

### 오늘의 작업

- 인증·카테고리·게시글·좋아요 API의 Controller, Service, Repository 및 통합 테스트를 작성하거나 보완했다.
- 기존 `.http` 파일의 시나리오를 자동 테스트로 전환하고 관련 파일 9개를 삭제했다.
- 테스트 전용 DB·JWT·파일 저장 환경을 구성하고 반복 실행 시 데이터와 파일이 남지 않도록 정리 방식을 적용했다.
- 테스트에서 발견된 미인증 응답의 명세 불일치를 승인된 범위에서 수정하고 관련 회귀 테스트를 강화했다.
- 작업 지침, 리뷰 기준, Issue·PR 템플릿과 README의 수동 API 확인 안내를 자동 테스트 기준으로 변경했다.

### 변경 내용

- Controller 테스트에서 요청 바인딩, Validation과 경계값, 응답 상태·본문 및 예외 응답을 검증했다.
- Service 테스트에서 인증, 게시글, 카테고리와 좋아요의 비즈니스 규칙 및 주요 실패 조건을 검증했다.
- Repository 테스트에서 H2 테스트 DB를 이용해 실제 저장·조회·수정·삭제, 사용자 정의 쿼리, 매핑과 관련 제약조건을 검증했다.
- `ApiIntegrationTest`에서 실제 Security·Service·Repository를 연결해 인증부터 DB 처리까지 API의 정상·주요 실패 흐름을 검증했다.
- 파일 저장 동작과 실제 HTTP 환경의 10MB 업로드 경계 및 초과 요청을 자동 테스트로 검증했다.
- 기존 DB 기반 `PostServiceTest` 시나리오 13개를 `PostServiceIntegrationTest`로 이동해 보존하고, `PostServiceTest`는 mock 기반 단위 테스트로 분리했다.
- 보호 API에서 토큰이 없거나 유효하지 않을 때 정확한 401 상태와 `{"status":401,"message":"인증이 필요합니다."}` JSON 응답을 반환하도록 `SecurityConfig`와 `ErrorCode`를 변경했다.
- JWT 발급·검증, 접근 허용 규칙 및 인증된 사용자의 작성자 불일치 403 정책은 변경하지 않았다.

### 자동 테스트 범위 / 실행 결과 / 미검증 사항

- 범위: 인증·카테고리·게시글·좋아요 API의 Controller, Service, Repository와 API 통합 흐름
- 추가 범위: 파일 저장, JWT 유효성·만료·잘못된 서명, 미인증 응답, 실제 HTTP 업로드 크기 제한, 테스트 데이터·파일 정리
- `.\gradlew.bat test`: 105개 실행, 실패 0, skip 0, `BUILD SUCCESSFUL`
- `.\gradlew.bat test --rerun-tasks`: 105개 재실행, 실패 0, skip 0, `BUILD SUCCESSFUL`
- `git diff --check`: 통과
- 반복 실행에서 데이터 또는 파일 잔존으로 인한 추가 실패가 발생하지 않았다.
- 기존 수동 API 확인은 실행하지 않았으며, 실제 HTTP 검증이 필요한 인증 응답과 업로드 제한은 RANDOM_PORT 기반 자동 테스트로 확인했다.
- H2 기반 테스트는 MySQL 고유 동작과 성능·부하·동시성 특성을 검증하지 않는다.
- 삭제된 게시글의 수정 허용 여부는 현재 명세에 정책이 명확하지 않아 검증 및 운영 코드 변경 대상에서 제외했다.
- CI 파이프라인 검증은 이번 Issue 범위에 포함하지 않았다.

### 문제 / 해결

- 최초 Gradle 실행이 sandbox 네트워크 제한으로 실패했다. 허용된 환경에서 다시 실행해 전체 테스트 결과를 확인했다.
- 계층 테스트 첫 실행에서는 매개변수 테스트의 배열 인자 전달 오류로 84개 중 6개가 실패했다. 테스트 인자를 개별 필드로 전달하도록 수정했다.
- 이후 전체 실행에서는 보호 API의 토큰 누락·무효 토큰 요청이 명세의 401이 아닌 403을 반환해 104개 중 10개가 실패했다. 기대값 완화나 테스트 비활성화 없이 사용자와 처리 방향을 협의했다.
- 승인 후 `AuthenticationEntryPoint`와 `UNAUTHORIZED` 오류 코드를 추가하고, 보호 API 5개에 대한 토큰 누락·무효 토큰 10개 시나리오에서 401, JSON 본문, Content-Type과 UTF-8을 검증했다.
- 인증 응답 수정 후 전체 테스트 105개와 반복 실행이 모두 통과했다. 공개 API, 로그인 실패 및 인증된 비작성자의 403 동작도 유지됨을 확인했다.
- 삭제된 게시글 수정은 현재 구현에서 삭제 여부를 확인하지 않지만 관련 정책이 명시되지 않았다. 이번 범위에서는 변경하지 않고 후속 검토 후보로 남겼다.

### AI 활용 기록

- Architect: Issue #33의 전체 API 테스트 범위, 계층별 검증 책임, 환경 격리 방식과 완료 조건을 `docs/current-task.md`에 정리했다.
- Builder: 자동 테스트 작성·보완, `.http` 파일 제거, 작업 지침 전환과 인증 응답 수정 작업을 수행하고 주요 결정·실패 원인·검증 결과를 `docs/current-work-log.md`에 기록했다.
- Architect와 사용자 협의: 테스트에서 발견된 미인증 401 명세와 실제 403 동작의 불일치를 숨기지 않고 보고한 뒤, 승인된 범위에서 인증 실패 응답을 수정했다.
- Reporter: Issue #33, PR #36 및 작업 로그를 대조해 테스트 범위, 트러블슈팅, 의사결정과 미검증 사항을 이 devlog 초안으로 정리했다.

### 다음 작업 후보

- PR #36 리뷰 결과 확인 및 필요한 후속 작업 정리
- 삭제된 게시글의 수정 허용 여부를 별도 정책 또는 Issue로 검토
- MySQL 고유 쿼리 동작이 중요해질 경우 실제 MySQL 기반 통합 테스트 도입 검토
- CI 파이프라인에서 전체 자동 테스트를 실행하는 별도 작업 검토

## 2026-09-16 - Issue #34 CI 구축 및 Codecov 커버리지 연동

### Issue / PR

- Issue: #34 feat: CI 구축 및 Codecov 커버리지 연동
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/34
- PR: #37 feat: CI 구축 및 Codecov 커버리지 연동
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/37
- PR 상태: OPEN
- Branch: `feature/issue-34-ci-codecov` → `develop`

### 오늘의 작업

- `develop` 대상 Pull Request와 `develop`·`main` 브랜치 push에서 전체 테스트를 실행하는 GitHub Actions CI를 구축했다.
- Gradle 테스트 완료 후 JaCoCo XML·HTML 리포트를 생성하고 XML 결과를 Codecov에 업로드하도록 구성했다.
- README에 `develop` 브랜치 기준 Codecov 배지를 추가했다.
- 최초 원격 CI 실패 원인을 수정하고, 후속 GitHub Actions 실행에서 테스트·리포트 생성·Codecov 업로드가 모두 성공하는 것을 확인했다.
- Architect 리뷰에서 확인된 배지 브랜치 기준과 검증 기록 문제를 반영하고 PR 본문과 작업 로그를 최신 상태로 갱신했다.

### 변경 내용

- `.github/workflows/ci.yml`에 Java 17과 Gradle Wrapper를 사용하는 CI workflow를 추가했다.
- workflow 실행 조건을 `develop` 대상 Pull Request 및 `develop`·`main` 브랜치 push로 설정했다.
- GitHub Actions 권한은 checkout에 필요한 `contents: read`로 제한했다.
- `build.gradle`에 JaCoCo 설정을 추가해 `test` 완료 후 XML과 HTML 커버리지 리포트를 생성하도록 했다.
- Codecov 업로드 전에 JaCoCo XML 파일의 존재를 확인하고, `CODECOV_TOKEN`을 사용해 업로드하도록 구성했다.
- `fail_ci_if_error: true`를 적용해 Codecov 업로드 오류가 발생하면 CI가 실패하도록 했다.
- 최소 커버리지 기준이나 커버리지 미달 실패 정책은 추가하지 않았다.
- README 배지 URL을 `develop` 브랜치 커버리지로 고정했다.
- Ubuntu 환경에서 Gradle Wrapper를 실행할 수 있도록 `gradlew` 실행 비트를 `100755`로 변경했다.
- API, Entity, DB, 인증·인가 및 기존 테스트 코드는 변경하지 않았다.

### 자동 테스트 범위 / 실행 결과 / 미검증 사항

- 범위: 기존 Controller, Service, Repository 및 API 통합 테스트 전체 105개
- 추가 검증 범위: `test` 이후 JaCoCo 리포트 생성, XML 파일 경로 확인, GitHub Actions 실행 조건·Java 버전·Gradle 명령·Codecov 설정 정적 검토
- 로컬 `.\gradlew.bat test`: 105개 실행, 실패 0, 오류 0, skip 0, `BUILD SUCCESSFUL`
- JaCoCo XML: `build/reports/jacoco/test/jacocoTestReport.xml`
- JaCoCo HTML: `build/reports/jacoco/test/html/index.html`
- GitHub Actions run `35093686170`: Gradle 테스트, `jacocoTestReport`, XML 파일 확인 및 Codecov 업로드 단계 성공
- `git diff --check`: 통과
- 신규 또는 변경된 API가 없어 Controller, Service, Repository 및 API 통합 테스트를 새로 추가하지 않았다.
- `develop` 및 `main` push trigger의 실제 원격 실행은 아직 확인하지 않았다.
- 수정된 README 배지가 실제로 `develop` 브랜치 커버리지를 표시하는지는 아직 확인하지 않았다.
- 로컬에 `actionlint` 또는 별도 YAML 파서가 없어 GitHub Actions 전용 스키마 검증은 수행하지 않았다.
- 발견된 명세 불일치는 없다.

### 문제 / 해결

- 최초 `git fetch`는 sandbox의 `.git` 쓰기 제한으로 실패했다. 승인된 권한으로 다시 실행해 `origin/develop`과의 차이가 없음을 확인한 뒤 작업 브랜치를 생성했다.
- 최초 로컬 Gradle 테스트는 sandbox 네트워크 제한으로 Gradle 9.4.1 배포본을 받지 못했다. 승인된 네트워크 권한으로 재실행해 전체 테스트 통과를 확인했다.
- PR #37의 최초 CI는 `./gradlew: Permission denied`와 종료 코드 126으로 테스트 시작 전에 실패했다. Git에 기록된 `gradlew` 모드가 `100644`인 것이 원인이었다.
- workflow에 별도 `chmod` 단계를 추가하는 대신 `gradlew` 실행 비트를 `100755`로 기록해 해결했다.
- Architect 리뷰에서 README 배지가 저장소 기본 브랜치를 가리키고, 작업 로그와 PR 본문에 성공한 원격 검증 결과가 반영되지 않은 문제가 확인됐다. 배지를 `develop` 브랜치로 고정하고 성공한 CI 결과를 두 기록에 반영했다.
- 사용자 결정에 따라 병합 후 `develop` 커버리지도 갱신할 수 있도록 기존 `main` push 조건에 `develop` push 조건을 추가했다.

### AI 활용 기록

- Architect: Issue #34의 CI 실행 조건, JaCoCo·Codecov 구성, 제외 범위와 완료 조건을 `docs/current-task.md`에 정리했다.
- Builder: GitHub Actions workflow, JaCoCo 설정, Codecov 업로드와 README 배지를 구현하고 로컬·원격 검증 결과 및 트러블슈팅을 `docs/current-work-log.md`에 기록했다.
- Architect 리뷰: README 배지의 브랜치 기준과 원격 CI 성공 결과의 기록 누락을 확인하고 필수 수정 사항을 제시했다.
- Builder와 사용자 협의: 배지를 `develop`으로 고정하고 CI push trigger를 `develop`과 `main`으로 확장했으며, 수정 후 원격 CI 성공 결과를 반영했다.
- Reporter: Issue #34, PR #37 및 작업 로그를 대조해 구현 내용, 주요 결정, 문제 해결 과정과 미검증 사항을 이 devlog 초안으로 정리했다.

### 다음 작업 후보

- PR #37의 최종 리뷰 결과 확인 및 병합 여부 결정
- 병합 후 `develop` push에서 CI와 Codecov 업로드가 실행되는지 확인
- 향후 `main` push 발생 시 CI와 Codecov 업로드가 정상 실행되는지 확인
- README 배지가 `develop` 브랜치의 실제 커버리지를 표시하는지 확인
- 필요하면 `actionlint`를 이용한 GitHub Actions workflow 스키마 검증 도입 검토
- 최소 커버리지 기준이나 커버리지 감소 실패 정책은 별도 Issue로 검토

## 2026-09-17 - Issue #27 댓글 CRUD·좋아요·정렬 및 페이징

### Issue / PR

- Issue: #27 [Feature] comment: 댓글 CRUD·좋아요·정렬 및 페이징
- Issue URL: https://github.com/marcel615/Ask-Teacher/issues/27
- PR: #38 [Feature] 댓글 CRUD·좋아요·정렬 및 페이징 구현
- PR URL: https://github.com/marcel615/Ask-Teacher/pull/38
- PR 상태: OPEN
- Branch: `feature/issue-27-comment-crud-likes` → `develop`

### 오늘의 작업

- 댓글 작성·목록 조회·수정·논리 삭제 API를 구현했다.
- 댓글 좋아요 등록·취소와 사용자별 중복 좋아요 방지를 구현했다.
- 댓글 목록의 최신순·좋아요순 정렬과 페이지 기반 페이징을 구현했다.
- 비로그인 공개 조회와 로그인 사용자의 `likedByMe` 계산을 적용했다.
- 댓글 기능의 Controller·Service·Repository 테스트와 JWT 인증을 포함한 API 통합 테스트를 추가했다.
- Architect 리뷰에서 확인된 JSON 오류 처리, Entity·ERD 스키마 정합성 및 작업 문서 누락을 보완했다.
- 리뷰의 선택 개선 사항을 반영해 시간 지연에 의존하던 통합 테스트를 제거하고 최신순 동률 정렬 테스트를 추가했다.

### 변경 내용

- `Comment`, `CommentLike` Entity와 댓글·좋아요 Controller, Service, Repository 및 Request·Response DTO를 추가했다.
- 댓글 목록 조회 시 `Comment`와 `CommentLike`를 조인·그룹화해 페이지 내 댓글별 추가 쿼리 없이 좋아요 수와 `likedByMe`를 함께 조회하도록 구현했다.
- 최신순은 `createdAt DESC`, `commentId DESC`, 좋아요순은 `likeCount DESC`, `createdAt DESC`, `commentId DESC`를 적용했다.
- 댓글 삭제 시 관련 좋아요는 물리 삭제하고 댓글은 논리 삭제하도록 같은 트랜잭션에서 처리했다.
- `(comment_id, user_id)` 유니크 제약과 `DataIntegrityViolationException` 변환을 통해 중복 좋아요를 `409 Conflict`로 처리했다.
- 댓글 목록은 비로그인 사용자에게 공개하고 작성·수정·삭제 및 좋아요 변경 API는 인증을 요구하도록 보안 설정을 반영했다.
- 빈 요청 본문과 잘못된 JSON에서 발생하는 `HttpMessageNotReadableException`을 공통 `INVALID_INPUT_VALUE` 형식의 `400 Bad Request`로 변환했다.
- `comments(post_id, deleted, created_at, id)` 인덱스와 `deleted DEFAULT false`를 Entity에 선언해 승인된 ERD와 JPA 생성 스키마를 일치시켰다.
- Issue #27 관련 요구사항, API 명세, ERD, 현재 작업 문서와 빠른 지시문을 반영했다.

### 자동 테스트 범위 / 실행 결과 / 미검증 사항

- Controller 테스트:
  - 댓글 요청의 JSON 바인딩, Validation, 기본 페이징·정렬, 응답 상태·필드 및 비즈니스 예외
  - 비로그인 댓글 목록 조회
  - 빈 본문과 잘못된 JSON의 공통 `400 Bad Request` 응답
- Service 테스트:
  - 댓글 작성·조회·수정·논리 삭제
  - 댓글 작성자 권한 검증
  - 삭제된 댓글 접근 차단
  - 좋아요 등록·중복 등록·취소
  - 댓글 삭제 시 관련 좋아요 제거
- Repository 테스트:
  - 삭제 댓글 제외
  - 최신순·좋아요순 및 동일 생성 시각의 `commentId DESC` 동률 정렬
  - 페이징과 전체 개수
  - 좋아요 집계와 `likedByMe`
  - `(comment_id, user_id)` 유니크 제약
  - 좋아요 일괄 삭제
  - H2 JDBC 메타데이터를 통한 comments 인덱스와 기본값 확인
- API 통합 테스트:
  - JWT 인증을 포함한 댓글 CRUD
  - 공개 댓글 목록과 로그인 사용자 `likedByMe`
  - 최신순·좋아요순 정렬과 페이징
  - Validation 및 작성자 권한 실패
  - 좋아요 등록·중복·취소
  - 삭제된 댓글 접근 실패
- 최초 댓글·보안 타깃 테스트: 23개 중 22개 성공, 1개 실패. 댓글 삭제 시 논리 삭제가 반영되지 않는 트랜잭션 문제를 발견했다.
- 트랜잭션 문제 수정 후 댓글 관련 타깃 테스트 23개가 모두 통과했다.
- PR 리뷰 반영 후 댓글 관련 타깃 테스트 24개가 모두 통과했다.
- PR 생성 전 전체 테스트: 125개 실행, 실패 0, 오류 0, skip 0, `BUILD SUCCESSFUL`.
- PR 리뷰 반영 후 `.\gradlew.bat test`: 129개 실행, 실패 0, 오류 0, skip 0, `BUILD SUCCESSFUL`.
- 최신 GitHub Actions CI `test`도 성공했다.
- `git diff --check`가 통과했다.
- 테스트 프로필의 H2와 격리된 애플리케이션 컨텍스트를 사용했으며, 통합 테스트마다 댓글 좋아요→댓글→기존 데이터 순으로 정리했다.
- MySQL 환경에서의 실제 JPQL 실행 계획과 성능은 검증하지 않았다.
- 실제 병렬 요청을 사용한 중복 좋아요 경합은 검증하지 않았다. DB 유니크 제약과 `DataIntegrityViolationException` 변환 단위 테스트로 대응 로직을 검증했다.
- 발견된 명세 불일치는 없다.

### 문제 / 해결

- 최초 `git fetch origin`이 sandbox의 `.git` 쓰기 제한으로 실패했다. 승인된 권한으로 다시 실행해 `develop`과 `origin/develop`의 차이가 없음을 확인했다.
- 최초 Gradle 테스트가 sandbox 네트워크 제한으로 배포본을 내려받지 못해 실패했다. 승인된 네트워크 권한으로 재실행해 테스트를 완료했다.
- 좋아요 일괄 삭제 쿼리의 `clearAutomatically=true`가 관리 중인 댓글 Entity를 분리해 댓글의 논리 삭제 상태가 저장되지 않았다. 자동 clear를 제거해 좋아요 물리 삭제와 댓글 논리 삭제가 같은 트랜잭션에서 반영되도록 수정했다.
- 댓글 작성·수정 요청의 빈 본문이나 잘못된 JSON이 공통 오류 형식으로 처리되지 않을 수 있었다. `HttpMessageNotReadableException` 처리와 Controller·통합 테스트를 추가해 `400 Bad Request` 응답을 고정했다.
- ERD에 정의된 comments 복합 인덱스와 `deleted` 기본값이 Entity 생성 스키마에 반영되지 않았다. Entity 선언을 보완하고 H2 JDBC 메타데이터로 실제 생성 결과를 검증했다.
- 통합 테스트의 `Thread.sleep(5)`가 시간 지연에 의존하고 있었다. sleep을 제거하고 동일 생성 시각의 `commentId DESC` 정렬을 Repository 테스트에서 결정적으로 검증했다.
- Architect 사전 반영 문서 목록에서 `docs/prompts/quick.md`가 누락돼 있었다. `docs/current-task.md`의 문서 목록과 개수를 실제 변경 범위에 맞게 정정했다.
- 댓글 기능과 관계없는 `PostRepository` 변경을 피하기 위해 기존 게시글 조회 메서드를 재사용하고 전체 테스트를 다시 실행했다.

### AI 활용 기록

- Architect: Issue #27의 기능 범위, API, Validation, 예외 처리, Entity·DB 구조, 테스트 조건과 제외 범위를 구체화하고 관련 설계 문서를 정리했다.
- Builder: 댓글 CRUD·좋아요·정렬·페이징을 구현하고 계층별 테스트와 API 통합 테스트를 작성했으며, 구현 중 트러블슈팅과 검증 결과를 `docs/current-work-log.md`에 기록했다.
- Architect 리뷰: 빈 본문·잘못된 JSON 처리, Entity·ERD 스키마 불일치, 사전 반영 문서 목록 누락을 수정 사항으로 확인했다. 시간 지연 제거와 최신순 동률 정렬 테스트 보완을 선택 개선 사항으로 제안했다.
- Builder: 리뷰 내용을 반영해 예외 처리, 생성 스키마, 문서 및 테스트를 보완하고 타깃 테스트 24개와 전체 테스트 129개 통과를 확인했다.
- Reporter: Issue #27, PR #38, PR 리뷰·CI 및 작업 로그를 대조해 구현 내용, 주요 결정, 트러블슈팅, 테스트 결과와 미검증 사항을 이 devlog 초안으로 정리했다.

### 다음 작업 후보

- PR #38의 리뷰 반영 결과를 최종 확인하고 병합 여부 결정
- PR 본문의 테스트 결과를 리뷰 반영 후 수치인 전체 129개로 갱신할지 검토
- MySQL 환경에서 댓글 목록 JPQL의 실제 실행 계획과 페이징 성능 검증
- 실제 병렬 요청을 사용한 중복 좋아요 경합 테스트를 별도 Issue로 검토
- 병합 후 `develop` 브랜치에서 전체 테스트와 CI 상태 확인
